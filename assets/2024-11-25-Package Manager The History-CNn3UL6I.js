var e={frontmatter:{title:`Package Manager: The History`,date:`2024-11-25T00:06:31`,tags:[`Code`],toc:!0,copyright:`CC BY-SA 4.0`,categories:[]},titleHtml:`Package Manager: The History`,html:`<p>This blog post was inspired by my classmate who encountered pip's dependencies hell when configuring machine learning python code. Yeah, for my academics-immersed amateur of industy, environment configuration and package managers are very unfamiliar things. It's not strange to crash into hell.</p>
<blockquote>
<p>Sometimes it’s simply impossible to find a combination of package versions that do not conflict. Welcome to\xA0<a href="https://en.wikipedia.org/wiki/Dependency_hell">dependency hell</a>.</p>
</blockquote>
<!-- more -->
<h2 id="the-era-without-package-managers"><a href="#the-era-without-package-managers">The era without package managers</a></h2>
<p>There was no so-called package manager for the early era programming languages. This can be seen from C and/or C++ (although for C family it is largely because C family programmers like to <a href="https://en.wikipedia.org/wiki/Reinventing_the_wheel">reinvent the wheel</a>, squeezing out the best performance). In this primitive period if you need to include something you must either use compiled dynamic link libraries or directly copy-paste the code into your own source code dir.</p>
<p>This method is obviously flawed. It is difficult to manage the versions of dependencies, lacking security updates, and the installation is cumbersome and prone to errors, etc. For this reason, package managers came into being.</p>
<h2 id="early-era-package-managers"><a href="#early-era-package-managers">Early era package managers</a></h2>
<p>Early package managers resolve dependencies and install packages in a certain location. This is based on the usual virtue of saving disk space. If two dependencies A and B both depend on a C, then we can obviously install only one copy of C for both A and B to use. Furthermore, if I have two projects that both depend on A, then putting A directly in the public directory e.g., python's <code>~/.local/lib/python3.12/site-packages</code> is also a good choice.</p>
<p>This has been the advantage of Linux for a long time. Due to historical reasons, Windows has long lacked a system built-in package manager and a unified installation location. Although it is still possible to reuse common libraries with dll, it is normaly time-consuming and laborious. Therefore, Windows software tends to prepare all the dlls it needs in its own directory, or even bundle them directly in the exe files. This makes Windows programs often larger than Linux programs.</p>
<p>But the virtue leads to a big problems, the <em>conflicting dependencies</em>.</p>
<p>This dependency issue arises when several packages have dependencies on the same\xA0<em>shared</em>\xA0packages or libraries, but they depend on different and incompatible versions of the shared packages. Solving the dependencies for one software may break the compatibility of another in a similar fashion to\xA0<a href="https://en.wikipedia.org/wiki/Whack-a-mole" title="Whack-a-mole">whack-a-mole</a>. If\xA0<code>app1</code>\xA0depends on\xA0<code>libfoo 1.2</code>, and\xA0<code>app2</code>\xA0depends on\xA0<code>libfoo 1.3</code>, and different versions of\xA0<code>libfoo</code>\xA0cannot be simultaneously installed, then\xA0<code>app1</code>\xA0and\xA0<code>app2</code>\xA0cannot simultaneously be used. <sup><a href="#user-content-fn-dephell" id="user-content-fnref-dephell" data-footnote-ref aria-describedby="footnote-label">1</a></sup></p>
<p>Unfortunately, many early era package managers simply could not install different versions of a library at the same time. What's even more terrifying is that pip, package manager of python the most popular language nowadays is among them.</p>
<p>In order to solve this problem, the package manager of programming languages has roughly derived several approaches. Here we introduce nodejs, docker and nix.</p>
<h2 id="package-management-of-nodejs"><a href="#package-management-of-nodejs">Package management of <a href="https://nodejs.org/en">Node.js</a></a></h2>
<p>In order to solve the coflicting dependencies hell, <a href="https://en.wikipedia.org/wiki/Npm">npm</a>, the default package manager of nodejs, chose to placed dependencies in a fixed folder <code>node_modules</code> of the project from the beginning. Each dependency has its own <code>node_modules</code>, so that dependencies do not interfere with each other.</p>
<p>For example if we want to add two dependencies A and B, but A depends to <code>C 1.0.0</code>, and B depends to <code>C 2.0.0</code>. Then the project's <code>node_modules</code> will probably look like this:</p>
<pre class="shiki github-light" style="background-color:#fff;color:#24292e" tabindex="0"><code><span class="line"><span>/node_modules</span></span>
<span class="line"><span>/node_modules/A</span></span>
<span class="line"><span>/node_modules/B</span></span>
<span class="line"><span>/node_modules/...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>/node_modules/A/node_modules/C@1.0.0</span></span>
<span class="line"><span>/node_modules/A/node_modules/... (other dependencies)</span></span>
<span class="line"><span>/node_modules/B/node_modules/C@2.0.0</span></span>
<span class="line"><span>/node_modules/B/node_modules/...</span></span></code></pre>
<pre class="shiki github-light" style="background-color:#fff;color:#24292e" tabindex="0"><code><span class="line"><span style="color:#D73A49">graph</span><span style="color:#6F42C1"> TD</span></span>
<span class="line"><span style="color:#E36209">Project </span><span style="color:#D73A49">--</span><span style="color:#D73A49">></span><span style="color:#E36209"> nm1</span><span style="color:#005cc5">[</span><span style="color:#032F62">node_modules</span><span style="color:#005cc5">]</span></span>
<span class="line"><span style="color:#E36209">nm1 </span><span style="color:#D73A49">--</span><span style="color:#D73A49">></span><span style="color:#E36209"> A</span></span>
<span class="line"><span style="color:#E36209">nm1 </span><span style="color:#D73A49">--</span><span style="color:#D73A49">></span><span style="color:#E36209"> B</span></span>
<span class="line"><span style="color:#E36209">A </span><span style="color:#D73A49">--</span><span style="color:#D73A49">></span><span style="color:#E36209"> nmA</span><span style="color:#005cc5">[</span><span style="color:#032F62">node_modules</span><span style="color:#005cc5">]</span></span>
<span class="line"><span style="color:#E36209">B </span><span style="color:#D73A49">--</span><span style="color:#D73A49">></span><span style="color:#E36209"> nmB</span><span style="color:#005cc5">[</span><span style="color:#032F62">node_modules</span><span style="color:#005cc5">]</span></span>
<span class="line"><span style="color:#E36209">nmA </span><span style="color:#D73A49">--</span><span style="color:#D73A49">></span><span style="color:#E36209"> c1</span><span style="color:#005cc5">[</span><span style="color:#032F62">C 1.0.0</span><span style="color:#005cc5">]</span></span>
<span class="line"><span style="color:#E36209">nmB </span><span style="color:#D73A49">--</span><span style="color:#D73A49">></span><span style="color:#E36209"> c2</span><span style="color:#005cc5">[</span><span style="color:#032F62">C 2.0.0</span><span style="color:#005cc5">]</span></span></code></pre>
<p>This effectively solves the problem of conflicts between sub-dependencies.</p>
<p>However, npm's approach also causes other problems.<sup><a href="#user-content-fn-these%20problems%20are%20generally%20solved%20in%20the%20new%20version%20(%E2%89%A57)%20of%20npm" id="user-content-fnref-these%20problems%20are%20generally%20solved%20in%20the%20new%20version%20(%E2%89%A57)%20of%20npm" data-footnote-ref aria-describedby="footnote-label">2</a></sup> One problem comes from a design flaw in the file system. Some systems, such as older versions of Windows, cannot handle path names longer than 255 characters. However, a path like <code>package_name/node_modules</code> often reaches 20 characters or more, causing problems with less than 10 levels of recursive dependencies. npm's approach also leads to a huge number of duplicate dependencies in the deep recursive dependency tree that people joke that node_modules is heavier than a black hole.</p>
<p><img src="/images/node_modules_black_hole.png" alt="node_modules heavier than black hole"></p>
<p>To solve these problems, the currently popular nodejs package manager, <a href="https://en.wikipedia.org/wiki/Pnpm">pnpm</a>, uses some clever ideas. pnpm will install the package locally shared, defaultly in <code>~/.pnpm-store</code>. Instead of storing complete dependencies in each project's <code>node_modules</code>, it will create symbolic links (symlinks) point to shared dependencies in the global store. At the same time, pnpm will flatten the dependencies, so dependency chains like <code>A -> B -> C -> D</code> will be flattened into node_modules in the root directory.</p>
<pre class="shiki github-light" style="background-color:#fff;color:#24292e" tabindex="0"><code><span class="line"><span>npm:</span></span>
<span class="line"><span>node_modules/A/node_modules/B/node_modules/C/node_modules/D/...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>pnpm:</span></span>
<span class="line"><span>node_modues/A</span></span>
<span class="line"><span>node_modues/B</span></span>
<span class="line"><span>node_modues/C</span></span>
<span class="line"><span>node_modues/D</span></span></code></pre>
<p>Also, pnpm use a strict dependency tree structure <code>pnpm-lock.yaml</code> to ensure that each package can only access its declared direct dependencies to prevent accidental access. (i.e, You didn't directly include <code>D</code> then you won't access <code>D</code> by <code>A -> B -> C -> D</code> chain)</p>
<p>Through these technologies, pnpm saves disk space and solves the problem of dependency hell to a large extent.</p>
<h2 id="virtual-environment-of-python"><a href="#virtual-environment-of-python">Virtual Environment of Python</a></h2>
<p>Languages ​​like Python choose to use virtual environments (venv) instead of improving pip's dependencies resolution architecture.</p>
<blockquote>
<p>The\xA0<code>venv</code>\xA0module supports creating lightweight “virtual environments”, each with their own independent set of Python packages installed in their\xA0<a href="https://docs.python.org/3/library/site.html#module-site" title="site: Module responsible for site-specific configuration."><code>site</code></a>\xA0directories. A virtual environment is created on top of an existing Python installation, known as the virtual environment’s “base” Python, and may optionally be isolated from the packages in the base environment, so only those explicitly installed in the virtual environment are available. <sup><a href="#user-content-fn-venv%20%E2%80%94%20creation%20of%20virtual%20environments%20%E2%80%94%20python%203.13.0%20documentation" id="user-content-fnref-venv%20%E2%80%94%20creation%20of%20virtual%20environments%20%E2%80%94%20python%203.13.0%20documentation" data-footnote-ref aria-describedby="footnote-label">3</a></sup></p>
</blockquote>
<p>In short, venv creates an independent, virtual runtime environment instead of using the common site-packages. In this way, projects using different virtual environments will never interfere with each other, solving the dependency conflict problem between different projects.</p>
<p>Unfortunately, python venv does not solve the dependency conflicts between sub-dependencies. Usually, pythoneans need to manually solve dependency problems, such as boldly adjusting dependency versions, directly forking confilcted dependencies, or nesting another venv in some venv.</p>
<h2 id="docker-the-last-solution"><a href="#docker-the-last-solution">Docker, the last solution</a></h2>
<p>Instead of working hard on algorithmic improvements on the package manager, Docker took a different approach. Docker packages the application into a container and runs it in a Linux virtual environment. Programs inside and outside the container can communicate with each other like ordinary programs through methods such as IPC communication and sockets.</p>
<p>Packaging an application into a container will involves not only the application itself, but all dependencies into a portable image. We don't need to worry about any dependency conflicts at all, because the inside of the docker container is more like an independent "lightweight virtual machine", and the docker containers will not interfere with each other in any way other than the pre-planned interface. Dependency handling is something that the publisher of the docker image needs to worry about, and as a user, you can use the image just after getting it, becaues everything in it is already packaged.</p>
<p>Of course, Docker also has huge drawbacks. The most obvious one is its huge space and resource usage, which is although obviously much smaller than installing a real virtual machine, but larger than a well-designed package manager installation. but hard disk space is becoming less valuable nowadays. If Docker can save 8 hours of painful installation and conflicted dependencies resolving process, what does it matter if there are just 500MB larger of space?</p>
<h2 id="further-nix"><a href="#further-nix">Further: Nix</a></h2>
<p>Nix is a\xA0<em>purely functional package manager</em>. This means that it treats packages like values in purely functional programming languages such as Haskell — they are built by functions that don’t have side-effects, and they never change after they have been built. Nix stores packages in the\xA0<em>Nix store</em>, usually the directory\xA0<code>/nix/store</code>, where each package has its own unique subdirectory such as <code>/nix/store/b6gvzjyb2pg0kjfwrjmg1vfhh54ad73z-firefox-33.1/</code> where\xA0<code>b6gvzjyb2pg0...</code>\xA0is a unique identifier for the package that captures all its dependencies (it’s a cryptographic hash of the package’s build dependency graph). This enables many powerful features.</p>
<blockquote>
<p>You can have multiple versions or variants of a package installed at the same time. This is especially important when different applications have dependencies on different versions of the same package — it prevents the “DLL hell”. Because of the hashing scheme, different versions of a package end up in different paths in the Nix store, so they don’t interfere with each other.</p>
<p>An important consequence is that operations like upgrading or uninstalling an application cannot break other applications, since these operations never “destructively” update or delete files that are used by other packages.</p>
</blockquote>
<p><a href="https://nixos.org/guides/how-nix-works/">How Nix Works | Nix &#x26; NixOS</a></p>
<hr>
<p>This blog post is licensed under CC-BY-SA 4.0. Sources are cited throughout the footnotes.</p>
<section data-footnotes class="footnotes"><h2 class="sr-only" id="footnote-label"><a href="#footnote-label">Footnotes</a></h2>
<ol>
<li id="user-content-fn-dephell">
<p><a href="https://en.wikipedia.org/wiki/Dependency_hell">Dependency hell - Wikipedia</a> <a href="#user-content-fnref-dephell" data-footnote-backref="" aria-label="Back to reference 1" class="data-footnote-backref">↩</a></p>
</li>
<li id="user-content-fn-these%20problems%20are%20generally%20solved%20in%20the%20new%20version%20(%E2%89%A57)%20of%20npm">
<p>These problems are generally solved in the new version (≥7) of npm <a href="#user-content-fnref-these%20problems%20are%20generally%20solved%20in%20the%20new%20version%20(%E2%89%A57)%20of%20npm" data-footnote-backref="" aria-label="Back to reference 2" class="data-footnote-backref">↩</a></p>
</li>
<li id="user-content-fn-venv%20%E2%80%94%20creation%20of%20virtual%20environments%20%E2%80%94%20python%203.13.0%20documentation">
<p><a href="https://docs.python.org/3/library/venv.html">venv — Creation of virtual environments — Python 3.13.0 documentation</a> <a href="#user-content-fnref-venv%20%E2%80%94%20creation%20of%20virtual%20environments%20%E2%80%94%20python%203.13.0%20documentation" data-footnote-backref="" aria-label="Back to reference 3" class="data-footnote-backref">↩</a></p>
</li>
</ol>
</section>`,headings:[{depth:2,slug:`the-era-without-package-managers`,text:`The era without package managers`},{depth:2,slug:`early-era-package-managers`,text:`Early era package managers`},{depth:2,slug:`package-management-of-nodejs`,text:`Package management of Node.js`},{depth:2,slug:`virtual-environment-of-python`,text:`Virtual Environment of Python`},{depth:2,slug:`docker-the-last-solution`,text:`Docker, the last solution`},{depth:2,slug:`further-nix`,text:`Further: Nix`},{depth:2,slug:`footnote-label`,text:`Footnotes`}]};export{e as default};